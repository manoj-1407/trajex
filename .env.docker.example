NODE_ENV=production
PORT=4000
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
CORS_ORIGIN=http://localhost:8080
FRONTEND_URL=http://localhost:8080

# Database credentials (for Docker)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=trajex_db
DATABASE_URL=postgresql://postgres:PASSWORD_HERE@db:5432/trajex_db

# Mailer (optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
