/**
 * useSocket hook — re-exports from SocketProvider for backward compatibility.
 * All components that import from './useSocket' will now get the shared socket.
 */
export { default } from './SocketProvider';
